{
  "name": "@app/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo build managed by wrangler",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo lint pendiente"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "@repo/shared": "*",
    "hono": "^4.10.6",
    "jose": "^6.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260320.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2",
    "wrangler": "^4.45.0"
  }
}