{
  "name": "@app/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "echo Usa 'vercel dev' desde la raiz del repositorio para simular Vercel",
    "build": "tsc -p tsconfig.json --noEmit",
    "deploy": "echo El despliegue se realiza desde Vercel",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo lint pendiente"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "@repo/shared": "*",
    "hono": "^4.10.6",
    "jose": "^6.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
